import { render, screen } from "utils/test-utils/render"
import {{pascalCase name}} from '../'

test("renders", () => {
    render(<{{pascalCase name}} />)
    expect(screen.getByText("{{pascalCase name}}"))
})